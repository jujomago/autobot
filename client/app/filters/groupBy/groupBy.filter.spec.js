'use strict';

describe('Filter: groupBy', function () {

  //TODO
  //This Filter uses lodash and doesn't works properly
  /*beforeEach(module('fakiyaMainApp'));

  // initialize a new instance of the filter before each test
  let groupBy, _lodash;
  let input = [
    {
    'app':{
      'appName': 'al', 
      'appFullName': 'Admin Console',
      'description': 'My app 1 description'
    },
    'partner':{
      'partnerName': 'f9',
      'partnerFullName': 'Five9'
    }  
    },
    {
      'app':{
        'appName': 'ra', 
        'appFullName': 'Reporting & Analytics',
        'description': 'Reporting & Analytics'
      },
      'partner':{
        'partnerName': 'f9',
        'partnerFullName': 'Five9'
      }
    }
  ];
  beforeEach(inject(function ($filter) {
    groupBy = $filter('groupBy');
  }));
  
  it('should return a object grouped By: partnerFullName', function () {
        
  });*/

});

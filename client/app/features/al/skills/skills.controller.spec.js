'use strict';
/*
describe('Component: SkillsComponent', function () {

  // load the controller's module
  beforeEach(module('fakiyaMainApp'));

  var SkillsComponent, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($componentController, $rootScope) {
    scope = $rootScope.$new();
    SkillsComponent = $componentController('al.skills', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).to.equal(1);
  });
});
*/